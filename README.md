# 💬 ChatRoom - Real-time Chat

Полнофункциональное приложение для real-time чата с поддержкой множества пользователей, построенное на современном стеке технологий.

**Live Demo:** https://simple-chat-iota-two.vercel.app/

## 📋 Содержание

- [Возможности](#возможности)
- [Технологии](#технологии)
- [Структура проекта](#структура-проекта)
- [Установка и запуск](#установка-и-запуск)
- [Архитектура](#архитектура)
- [API и Socket Events](#api-и-socket-events)
- [Деплой](#деплой)
- [Переменные окружения](#переменные-окружения)
- [Разработка](#разработка)

## ✨ Возможности

### Основные функции
- ✉️ **Real-time обмен сообщениями** - мгновенная доставка через WebSockets
- 👥 **Множество пользователей** - одновременная работа неограниченного числа пользователей
- 🟢 **Индикатор подключения** - визуальный статус соединения с сервером
- 📱 **Адаптивный дизайн** - работает на всех устройствах
- ⚡ **Быстрая загрузка** - оптимизированная сборка с Vite

### Новые улучшения
- 💾 **Постоянная история** - все сообщения сохраняются в MongoDB
- 👥 **Список пользователей онлайн** - боковая панель с активными пользователями
- ✍️ **Typing indicator** - показывает когда кто-то печатает
- 🎨 **Аватары пользователей** - цветные аватары с инициалами
- ⏰ **Временные метки** - отображение времени отправки каждого сообщения
- 🌈 **Улучшенный дизайн** - градиенты, анимации, плавные переходы
- 🎬 **Анимации** - плавное появление сообщений и элементов
- 📜 **Кастомный скроллбар** - красивая прокрутка в стиле приложения

## 🛠 Технологии

### Backend
- **Node.js** - серверная платформа JavaScript
- **Express** - веб-фреймворк для Node.js
- **Socket.io** - библиотека для real-time двунаправленной связи
- **MongoDB** - NoSQL база данных для хранения истории сообщений
- **Mongoose** - ODM для работы с MongoDB
- **CORS** - middleware для кросс-доменных запросов

### Frontend
- **React 18** - библиотека для построения UI
- **Vite** - быстрый инструмент сборки
- **Tailwind CSS** - utility-first CSS фреймворк
- **Socket.io Client** - клиент для WebSocket соединений

### DevOps
- **Git** - система контроля версий
- **GitHub** - хостинг кода
- **Vercel** - хостинг frontend (бесплатно)
- **Render** - хостинг backend (бесплатно)

## 📁 Структура проекта

```
simple-chat/
├── backend/                 # Backend сервер
│   ├── index.js            # Основной файл сервера
│   ├── package.json        # Зависимости backend
│   └── .env.example        # Пример переменных окружения
│
├── frontend/               # Frontend приложение
│   ├── src/
│   │   ├── App.jsx        # Главный компонент приложения
│   │   ├── main.jsx       # Точка входа React
│   │   └── index.css      # Глобальные стили
│   ├── index.html         # HTML шаблон
│   ├── package.json       # Зависимости frontend
│   ├── vite.config.js     # Конфигурация Vite
│   ├── tailwind.config.js # Конфигурация Tailwind
│   ├── postcss.config.js  # Конфигурация PostCSS
│   └── .env.example       # Пример переменных окружения
│
├── .gitignore             # Игнорируемые файлы Git
└── README.md              # Документация проекта
```

## 🚀 Установка и запуск

### Предварительные требования

- Node.js >= 14.0.0
- npm >= 6.0.0
- Git

### Клонирование репозитория

```bash
git clone https://github.com/bekaisatova-ai/simple-chat.git
cd simple-chat
```

### Запуск Backend

```bash
cd backend
npm install
npm start
```

Backend запустится на `http://localhost:3001`

### Запуск Frontend

Откройте новый терминал:

```bash
cd frontend
npm install
npm run dev
```

Frontend откроется на `http://localhost:5173`

### Локальный доступ из сети

Для доступа с других устройств в локальной сети:

```bash
# В frontend
npm run dev -- --host
```

Приложение будет доступно по вашему локальному IP (например, `http://192.168.1.x:5173`)

## 🏗 Архитектура

### Backend (Socket.io Server)

Backend построен на Express и Socket.io, обеспечивает:

1. **WebSocket соединения** - постоянное двунаправленное соединение с клиентами
2. **Управление пользователями** - отслеживание подключенных пользователей через Map
3. **Хранение сообщений** - в памяти (последние 50 сообщений)
4. **CORS** - разрешение запросов с frontend домена

**Основные компоненты:**

```javascript
// Хранилище
let messages = [];           // Массив сообщений
let users = new Map();       // Карта пользователей (socketId -> username)

// Socket.io события
io.on('connection', ...)     // Новое подключение
socket.on('join', ...)       // Пользователь присоединился
socket.on('message', ...)    // Новое сообщение
socket.on('disconnect', ...) // Пользователь отключился
```

### Frontend (React + Socket.io Client)

Frontend построен на React с хуками, обеспечивает:

1. **Управление состоянием** - useState для локального состояния
2. **WebSocket подключение** - useEffect для управления соединением
3. **Отзывчивый UI** - Tailwind CSS для стилей
4. **Авто-скролл** - useRef для управления DOM

**Основные компоненты:**

```javascript
// Состояния
const [username, setUsername]     // Имя пользователя
const [joined, setJoined]         // Флаг входа в чат
const [messages, setMessages]     // Список сообщений
const [input, setInput]           // Текст в поле ввода
const [connected, setConnected]   // Статус подключения

// Refs
const socketRef                   // Ссылка на Socket.io клиент
const messagesEndRef              // Ссылка для автопрокрутки
```

## 🔌 API и Socket Events

### Socket Events (Backend → Frontend)

#### `connect`
Событие подключения к серверу.

```javascript
socket.on('connect', () => {
  console.log('Connected to server');
});
```

#### `history`
Получение истории сообщений при входе.

```javascript
socket.on('history', (messages) => {
  // messages: Array<{id, username, text, timestamp}>
});
```

#### `message`
Получение нового сообщения.

```javascript
socket.on('message', (message) => {
  // message: {id, username, text, timestamp}
});
```

#### `user-joined`
Уведомление о входе нового пользователя.

```javascript
socket.on('user-joined', ({username, userId}) => {
  console.log(`${username} joined the chat`);
});
```

#### `user-left`
Уведомление о выходе пользователя.

```javascript
socket.on('user-left', ({username, userId}) => {
  console.log(`${username} left the chat`);
});
```

#### `online-users`
Получение списка пользователей онлайн.

```javascript
socket.on('online-users', (users) => {
  // users: Array<{username, avatar}>
});
```

#### `typing`
Получение списка пользователей, которые печатают.

```javascript
socket.on('typing', (typingUsers) => {
  // typingUsers: Array<string> - массив имен пользователей
});
```

#### `disconnect`
Отключение от сервера.

```javascript
socket.on('disconnect', () => {
  console.log('Disconnected from server');
});
```

### Socket Events (Frontend → Backend)

#### `join`
Присоединение к чату с именем пользователя.

```javascript
socket.emit('join', username);
```

#### `message`
Отправка сообщения.

```javascript
socket.emit('message', {text: 'Hello!'});
```

#### `typing`
Уведомление о том, что пользователь печатает.

```javascript
socket.emit('typing', true);  // Начал печатать
socket.emit('typing', false); // Закончил печатать
```

## 🌐 Деплой

### Деплой на Render (Backend)

1. Создайте аккаунт на [Render](https://render.com/)
2. Подключите ваш GitHub репозиторий
3. Создайте новый Web Service:
   - **Name**: `simple-chat-backend`
   - **Root Directory**: `backend`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Instance Type**: Free
4. Добавьте переменную окружения:
   - `FRONTEND_URL`: URL вашего frontend

### Деплой на Vercel (Frontend)

1. Создайте аккаунт на [Vercel](https://vercel.com/)
2. Импортируйте GitHub репозиторий
3. Настройте проект:
   - **Root Directory**: `frontend`
   - **Framework**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
4. Добавьте переменную окружения:
   - `VITE_BACKEND_URL`: URL вашего backend

### Автоматический деплой

После настройки, каждый `git push` автоматически запустит новый деплой на Vercel и Render.

## 🔐 Переменные окружения

### Backend (.env)

```env
PORT=3001                                       # Порт сервера (опционально)
FRONTEND_URL=https://your-frontend.vercel.app  # URL frontend для CORS
MONGODB_URI=mongodb://localhost:27017/simple-chat  # URL MongoDB (опционально, работает без БД)
```

**Примечание:** Если MongoDB не установлен, приложение будет работать в режиме хранения в памяти.

### Frontend (.env)

```env
VITE_BACKEND_URL=https://your-backend.onrender.com  # URL backend API
```

**Примечание:** Файлы `.env` не включены в репозиторий. Используйте `.env.example` как шаблон.

## 💻 Разработка

### Установка зависимостей

```bash
# Backend
cd backend
npm install

# Frontend
cd frontend
npm install
```

### Скрипты разработки

**Backend:**
```bash
npm start          # Запуск сервера
```

**Frontend:**
```bash
npm run dev        # Запуск dev сервера
npm run build      # Сборка для production
npm run preview    # Превью production сборки
```

### Обновление кода на продакшене

```bash
# 1. Внесите изменения в код
# 2. Закоммитьте изменения
git add .
git commit -m "Описание изменений"

# 3. Отправьте на GitHub
git push

# 4. Vercel и Render автоматически задеплоят обновления
```

### Советы по разработке

- **Hot Reload**: Vite автоматически перезагружает страницу при изменениях
- **Console**: Используйте browser DevTools для отладки frontend
- **Logs**: Проверяйте логи backend в терминале или на Render Dashboard
- **CORS**: При локальной разработке frontend должен быть на `localhost:5173`

## 🐛 Отладка

### Backend не запускается

- Проверьте, свободен ли порт 3001
- Убедитесь, что установлены все зависимости (`npm install`)
- Проверьте версию Node.js (`node -v`)

### Frontend не подключается к Backend

- Проверьте, что backend запущен
- Убедитесь, что `VITE_BACKEND_URL` указывает на правильный URL
- Проверьте CORS настройки на backend
- Откройте Browser DevTools → Network → WS для проверки WebSocket соединения

### Сообщения не отправляются

- Проверьте индикатор подключения (зеленая/красная точка)
- Откройте Browser Console для ошибок
- Проверьте, что Socket.io соединение установлено

## 📝 Лицензия

Этот проект создан в учебных целях и доступен для свободного использования.

## 👤 Автор

**Begaiym**
- GitHub: [@bekaisatova-ai](https://github.com/bekaisatova-ai)

## 🙏 Благодарности

- Socket.io за отличную библиотеку real-time коммуникации
- React команда за мощный UI фреймворк
- Vercel и Render за бесплатный хостинг

---

**ChatRoom - Создано с ❤️ используя React, Node.js и Socket.io**
